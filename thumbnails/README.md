# IRIAMサムネイル

AKI本人が採用したIRIAM用サムネイルを、立ち絵マスターや生成候補とは分離して保存します。

## 保存ルール

- 採用画像は日付ごとのフォルダへ保存する
- 高解像度の制作原本と、アップロード用画像を両方残す
- 採用画像を上書き・削除しない
- 立ち絵の正式頭部マスターや全身マスターへ昇格・転用しない
- 顔・髪の固定基準は `master-spec/` の現行資料を参照する

